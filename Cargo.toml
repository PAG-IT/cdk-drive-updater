[package]
name = "cdk-drive-updater"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
chrono = { version = "0.4", default-features = false, features = ["clock"] }
dotenvy = "0.15"
fern = "0.7"
log = "0.4"
